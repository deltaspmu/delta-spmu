# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
dist-ssr/
build/
*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Environment variables
.env
.env.local
.env.*.local
*.env
# ...but the committed per-environment deploy config is NOT secret (no keys in it)
!scripts/env/*.env

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
.eggs/
*.egg
venv/
.venv/
pip-log.txt

# Terraform
infrastructure/.terraform/
infrastructure/*.tfstate
infrastructure/*.tfstate.*
infrastructure/*.tfvars
infrastructure/.terraform.lock.hcl
infrastructure/tfplan
infrastructure/*.tfplan
infrastructure/lambda_placeholder.zip
*.tfstate
*.tfstate.*
*.tfvars
*.tfplan

# IDE
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Vercel
.vercel/

# Claude Code per-user settings (each user has their own)
.claude/settings.local.json

# Misc
*.tsbuildinfo
coverage/
.cache/
infrastructure/**/lambda_placeholder.zip

# Playwright CLI run artifacts (per-run console logs + page snapshots)
.playwright-cli/

# Local-only staging E2E report — documents live security weaknesses; must not
# be committed to this public repo. Keep on disk for reference only.
docs/STAGING_E2E_TEST_REPORT.md
